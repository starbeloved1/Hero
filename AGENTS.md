# Hero 协作入口

本文件面向在 `Hero/` 内工作的协作代理，规定执行顺序和边界；代码与接口规范见 `heros/docs/STANDARD.md`，项目当前状态见 `heros/docs/MEMORY.md`。

## 开始工作前

1. 阅读 `heros/docs/MEMORY.md`，确认当前进度、环境限制和下一步。
2. 阅读 `heros/docs/STANDARD.md`，遵守稳定的工程规范。
3. 查看 Git 状态，保留并避开用户已有的无关改动。

## 工作边界

- `2026HeroAim/` 是只读兼容性基准，禁止修改；迁移时保持其协议、标定含义、公开行为和算法结果。
- `../ly_aim/` 是 ROS 设计参考，可借鉴包拆分、参数、launch、测试与硬件生命周期；不得成为运行时依赖，也不得照搬后改变 Hero 行为。
- 当前处于 ROS 迁移阶段：按可验证的小切片推进。每个切片完成实现、构建和测试后，先带用户读懂代码，再开始下一切片。
- 迁移完成后的优化、调试、部署和维护任务按用户明确的目标与风险范围执行，不强行套用迁移切片流程。
- SDK 或真实硬件缺失时，不得伪造设备可用；实现可离线验证的边界，并如实记录限制。

## 交接要求

- 修改后按风险完成构建、测试和必要的运行验证。
- 发现或改变当前状态、环境限制、接口决定、迁移进度时，更新 `MEMORY.md`。
- 仅当稳定工程规范发生变化时更新 `STANDARD.md`。

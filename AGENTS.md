# Hero 协作入口

本文件面向在 `Hero/` 内工作的协作代理，规定执行顺序和边界；代码与接口规范见 `heros/docs/STANDARD.md`，项目当前状态见 `heros/docs/MEMORY.md`。

## 开始工作前

1. 阅读 `heros/docs/MEMORY.md`，确认当前进度、环境限制和下一步。
2. 阅读 `heros/docs/STANDARD.md`，遵守稳定的工程规范。
3. 查看 Git 状态，保留并避开用户已有的无关改动。

## 工作边界

- `2026HeroAim/` 是只读且相对稳定的行为兼容性基准，禁止修改；在验证、调参和优化 Hero 时必须持续对照其协议、标定含义、公开行为和算法结果。
- `../ly_aim/` 是优秀的 ROS 案例，必须主动借鉴其包拆分、参数、launch、测试、硬件生命周期和处理思路；不得成为运行时依赖，也不得照搬后改变 Hero 行为。
- ROS 功能块的初步迁移已完成。当前阶段是熟悉代码和通信流程，以录像与实车验证检测稳定性，学习读取可视化和 debug 数据调参，并在实证基础上逐步优化现有代码；不得因旧迁移流程而无目标地大规模重构。
- SDK 或真实硬件缺失时，不得伪造设备可用；实现可离线验证的边界，并如实记录限制。

## 代码讲解约定

- 用户当前采用“先结构、后细节”的阅读方式。第一次讲解一个功能域或功能包时，先建立完整调用与通信地图，不提前陷入公式、底层实现或单行语法细节。
- 第一次经过每个文件时，必须说明：该文件在整体中的职责；YAML 的全部参数及其归属；订阅和发布的 topic、消息类型与数据方向；触发该文件行为的回调、定时器或调用关系；每个函数的职责及对应行为；与旧工程的关系（存在时）。
- 所有关联文件完成第一遍结构阅读后，再由用户指定需要深入的算法、数学、时间戳、并发或具体实现细节。

## 交接要求

- 修改后按风险完成构建、测试和必要的运行验证。
- 发现或改变当前状态、环境限制、接口决定、迁移进度时，更新 `MEMORY.md`。
- 仅当稳定工程规范发生变化时更新 `STANDARD.md`。
